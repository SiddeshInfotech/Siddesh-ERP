/**
 * PLACEHOLDER — replaced by BE-19.
 *
 * Generate with:
 *   npm run db:types
 * or against the hosted project:
 *   npx supabase gen types typescript --project-id <ref> > packages/shared/database.types.ts
 *
 * This file is committed on purpose: it is the compile-time contract both the
 * desktop and mobile clients build against.
 */
export type Database = Record<string, never>
